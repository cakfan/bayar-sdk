# @bayar-sdk/xendit

## 0.0.1

### Patch Changes

- fix(xendit): use Payment Request API `channel_code` request body (`request_amount`/`channel_code`/`channel_properties`) so `createCharge` works against the real API
