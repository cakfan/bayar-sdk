{
	"name": "@bayar-sdk/example-node-basic",
	"version": "0.0.0",
	"private": true,
	"description": "Basic usage example: MidtransProvider directly (createCharge, getCharge, refund, parseWebhook)",
	"type": "module",
	"scripts": {
		"start": "bun --env-file=../../.env run src/index.ts",
		"typecheck": "tsc --noEmit"
	},
	"dependencies": {
		"@bayar-sdk/core": "workspace:*",
		"@bayar-sdk/midtrans": "workspace:*"
	},
	"devDependencies": {
		"@types/bun": "^1.3.14",
		"typescript": "^5.9.3"
	}
}
